worker_processes  1;
error_log logs/error.log;
events {
    worker_connections 1024;
}
http {
    server {
		listen <%= p("webapp.port") %>;
		server_name <%= p("webapp.servername") %>;

		location / {
			root /var/vcap/jobs/webapp/htdocs;
		}
    }
}
